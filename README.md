# Sabia Srl — GitHub Pages site

Static site: edit `index.html`, `ict_consulting.html`, and styles in `assets/css/main2.css` (plus `noscript.css` and Font Awesome as needed).

Embedded mini-sites live under `apps/` and `web/`; leave those trees as-is for deployed URLs.

**Theme:** Hyperspace by [HTML5 UP](https://html5up.net) (CCA 3.0 — see `LICENSE.txt`).
